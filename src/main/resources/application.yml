server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/car_rental
    username: ${DB_USERNAME:car_rental}
    password: ${DB_USERNAME:car_rental}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
